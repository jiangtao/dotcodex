{
  "name": "dotcodex",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Codex-native workflow repository with a stable public skill distribution",
  "scripts": {
    "test": "node --test tests/*.test.mjs tests/**/*.test.mjs",
    "build:skills": "node scripts/cli.mjs build-public",
    "install:skills": "node scripts/install.mjs",
    "sync:superpowers": "sh scripts/sync-superpowers-from-dotclaude.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
